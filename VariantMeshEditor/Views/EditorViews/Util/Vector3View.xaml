<UserControl x:Class="VariantMeshEditor.Views.EditorViews.Util.Vector3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="self">
    <Grid>
        <GroupBox Header="{Binding VectorData.Name, ElementName=self, UpdateSourceTrigger=PropertyChanged}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                    <TextBox Text="{Binding VectorData.X, ElementName=self, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat=N5}" Width="50" Margin="2,0,2,0"></TextBox>
                    <TextBox Text="{Binding VectorData.Y, ElementName=self, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat=N5}" Width="50" Margin="2,0,2,0"></TextBox>
                    <TextBox Text="{Binding VectorData.Z, ElementName=self, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat=N5}" Width="50" Margin="2,0,2,0"></TextBox>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
