<UserControl x:Class="VariantMeshEditor.Views.EditorViews.SkeletonEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:commondialogs="clr-namespace:CommonDialogs;assembly=CommonDialogs"
             mc:Ignorable="d" 
             x:Name="Self"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="Skeleton Name:" Width="100"/>
            <Label Content="{Binding SkeletonName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="Bone Count:" Width="100"/>
            <Label Content="{Binding BoneCount, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <TreeView DockPanel.Dock="Top" x:Name="SkeletonBonesView"></TreeView>
    </DockPanel>

</UserControl>
