<UserControl x:Class="VariantMeshEditor.Views.EditorViews.ToolPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VariantMeshEditor.Views.EditorViews" xmlns:viewmodels="clr-namespace:VariantMeshEditor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{ x:Type viewmodels:AnimationElement}">
            <local:AnimationEditorView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type viewmodels:SlotElement}">
            <local:SlotEditorView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:RigidModelElement}">
            <local:RigidModelEditorView/>
        </DataTemplate>
    </UserControl.Resources>

    <DockPanel>
        <ContentControl Content="{Binding Path=SelectedNode, UpdateSourceTrigger=PropertyChanged}" />
    </DockPanel>
    
</UserControl>
