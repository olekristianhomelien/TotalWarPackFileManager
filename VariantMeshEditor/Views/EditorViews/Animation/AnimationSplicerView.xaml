<UserControl x:Class="VariantMeshEditor.Views.EditorViews.Animation.AnimationSplicerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VariantMeshEditor.Views.EditorViews.Animation" xmlns:filterdialog="clr-namespace:CommonDialogs.FilterDialog;assembly=CommonDialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TreeViewItemFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <PathGeometry x:Key="TreeArrow" Figures="M0,0 L0,6 L6,0 z"/>

        <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border Width="16" Height="16" Background="Transparent" Padding="5,5,5,5">
                            <Path x:Name="ExpandPath" Fill="Transparent" Stroke="#FF989898" Data="{StaticResource TreeArrow}">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="135" CenterX="3" CenterY="3"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Stroke" TargetName="ExpandPath" Value="#FF1BBBFA"/>
                                <Setter Property="Fill" TargetName="ExpandPath" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="RenderTransform" TargetName="ExpandPath">
                                    <Setter.Value>
                                        <RotateTransform Angle="180" CenterX="3" CenterY="3"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Fill" TargetName="ExpandPath" Value="#FF595959"/>
                                <Setter Property="Stroke" TargetName="ExpandPath" Value="#FF262626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TreeViewItemStyle1" TargetType="{x:Type TreeViewItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding Path=HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding Path=VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Padding" Value="1,0,0,0"/>
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource TreeViewItemFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander" Style="{StaticResource ExpandCollapseToggleStyle}" ClickMode="Press" IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Border x:Name="Bd" SnapsToDevicePixels="true" Grid.Column="1" Grid.ColumnSpan="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter x:Name="PART_Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header"/>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>


    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <TextBlock>Target Skeleton -> </TextBlock>
            <TextBlock>Humanoid01</TextBlock>
        </StackPanel>


        <filterdialog:CollapsableFilterControl  
                SelectedItem="{Binding SelectedAnimationPackFile, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                SearchItems="{Binding FilterList, UpdateSourceTrigger=PropertyChanged}"     
                OnSearch="{Binding FilterItemOnSearch, UpdateSourceTrigger=PropertyChanged}"   
                IsManipulationEnabled="False"
                LabelTotalWidth="100"   
                DisplayMemberPath="FullPath"
                DockPanel.Dock="top"
                MaxHeight="400"
                LabelText="Target Anim">
            <filterdialog:CollapsableFilterControl.InnerContent>
                <CheckBox 
                        IsChecked="{Binding OnlyDisplayAnimationsForCurrentSkeleton, UpdateSourceTrigger=PropertyChanged}"
                        Content="Only list animations for current skeleton"/>
            </filterdialog:CollapsableFilterControl.InnerContent>
        </filterdialog:CollapsableFilterControl>



        <filterdialog:CollapsableFilterControl  
                SelectedItem="{Binding SelectedAnimationPackFile, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                SearchItems="{Binding FilterList, UpdateSourceTrigger=PropertyChanged}"     
                OnSearch="{Binding FilterItemOnSearch, UpdateSourceTrigger=PropertyChanged}"   
                IsManipulationEnabled="False"
                LabelTotalWidth="100"   
                DisplayMemberPath="FullPath"
                DockPanel.Dock="top"
                MaxHeight="400"
                LabelText="External Skeleton">
        </filterdialog:CollapsableFilterControl>


        <filterdialog:CollapsableFilterControl  
                SelectedItem="{Binding SelectedAnimationPackFile, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                SearchItems="{Binding FilterList, UpdateSourceTrigger=PropertyChanged}"     
                OnSearch="{Binding FilterItemOnSearch, UpdateSourceTrigger=PropertyChanged}"   
                IsManipulationEnabled="False"
                LabelTotalWidth="100"   
                DisplayMemberPath="FullPath"
                DockPanel.Dock="top"
                MaxHeight="400"
                LabelText="External Anim">
                <filterdialog:CollapsableFilterControl.InnerContent>
                    <CheckBox 
                        IsChecked="{Binding OnlyDisplayAnimationsForCurrentSkeleton, UpdateSourceTrigger=PropertyChanged}"
                        Content="Only list animations for current skeleton"/>
                </filterdialog:CollapsableFilterControl.InnerContent>
            </filterdialog:CollapsableFilterControl>
      
        
        



        <TreeView DockPanel.Dock="Top" HorizontalContentAlignment="Stretch" Name="DTV" ItemsSource="{Binding Bones}" 
              ItemContainerStyle="{StaticResource TreeViewItemStyle1}" >


            <TreeView.ItemTemplate>

                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" SharedSizeGroup="DDa"  />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />


                        </Grid.ColumnDefinitions>
                        <TextBlock  Grid.Column="0" Text="{Binding BoneName}"   />
                        <ComboBox  Width="150"   Grid.Column="1"  HorizontalContentAlignment="Stretch" Margin="10,0,0,0">
                            <ComboBoxItem IsSelected="true">Copy_all</ComboBoxItem>
                            <ComboBoxItem>Copy_transform</ComboBoxItem>
                            <ComboBoxItem>Copy_IncludeParent</ComboBoxItem>
                        </ComboBox>

                        <ComboBox  Width="150"  Grid.Column="2"  Margin="10,0,0,0">
                            <ComboBoxItem>animroot</ComboBoxItem>
                            <ComboBoxItem>root</ComboBoxItem>
                            <ComboBoxItem>Spine_0</ComboBoxItem>
                            <ComboBoxItem>Spine_1</ComboBoxItem>
                            <ComboBoxItem>Spine_2</ComboBoxItem>
                            <ComboBoxItem>Clav_left</ComboBoxItem>
                            <ComboBoxItem>ork_left_arm</ComboBoxItem>

                        </ComboBox>
                        <Button Grid.Column="3" Margin="10,0,0,0">Browse</Button>

                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>




    </DockPanel>
</UserControl>
