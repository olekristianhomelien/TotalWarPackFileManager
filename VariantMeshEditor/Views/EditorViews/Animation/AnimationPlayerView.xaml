<UserControl x:Class="VariantMeshEditor.Views.EditorViews.AnimationViews.AnimationPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    
    <DockPanel LastChildFill="False">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Center">
            <TextBlock Text="Frame"/>
            <TextBlock Text="{Binding CurrentFrame, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text=" / "/>
            <TextBlock Text="{Binding MaxFames, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Center">
            <Button Command="{Binding PrivFrameCommand}" FontSize="20">⏮︎</Button>
            <Button Command="{Binding PausePlayCommand}" FontSize="20">⏯︎</Button>
            <Button Command="{Binding NextFrameCommand}" FontSize="20">⏭︎</Button>
            <ComboBox 
                ItemsSource="{Binding PossibleAnimationSpeeds}" 
                SelectedItem="{Binding SelectedAnimationSpeed, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                DisplayMemberPath="DisplayName"
                VerticalContentAlignment="Center"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Center">
            <CheckBox IsChecked="{Binding SyncAllAnimations, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Content="Sync Animations"/>
            <CheckBox IsChecked="{Binding AnimateInPlace, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"    Content="Animate in place"/>
        </StackPanel>
    </DockPanel>
</UserControl>
